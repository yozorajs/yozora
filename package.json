{
  "name": "@yozora/yozora",
  "version": "0.0.0",
  "author": "lemon-clown <me.clown.lemon@gmail.com>",
  "repository": "https://github.com/lemon-clown/saturn.git",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean --yes",
    "build": "lerna exec -- yarn build",
    "postbuild": "lerna exec -- yarn clean:build",
    "lint:fix": "lerna exec -- yarn lint:fix",
    "test": "lerna exec -- yarn test"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
